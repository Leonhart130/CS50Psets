take for argv[1] an integer

if argc != 2
    fprintf error message
    return 1;

if argv[1] is not an integer
    fprintf error message
    return 2;

string userInput = get_string("text: ");

fprintf(stdout, "%s\n", ciphering(key, plaintext);



char *ciphering(int key, string plaintext)

    char *cipheredText = malloc((sizeof(char) * strlen(plaintext)) + 1);

    for (int i = 0, n = strlen(plaintext); i < n; i++)
    {
        if (isalpha)
        {
            if (isupper..)
            {

            }

            else
            {

            }
        }
        else
        {
            *cipheredText[i] = plaintext[i];
        }

    }
    return cipheredText;
